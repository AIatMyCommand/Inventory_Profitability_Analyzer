# Task #1
# intialize db file
# create table
# add some items

# Task #2 - build the item-list query

# Task #3 - display list of items in tkinter
# 3.1 paginate (display only 10 entries with previous/next buttons)

# 4 - add `edit` + `delete` buttons on each line

# 5 build the update query

# 6 build the update view (take your `new` view and pre-fill with entry's values)


# Create
# Read -> list
# Update -> create view + populated data, query, confirmation dialog(++)
# Delete -> confirmation dialog, query
